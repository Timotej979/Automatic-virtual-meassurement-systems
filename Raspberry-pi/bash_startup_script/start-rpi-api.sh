#!/bin/bash
# Go to the directory where the script is located and build the docker image and run the container
cd ~/Documents/Automatic-virtual-meassurement-systems/Raspberry-pi && docker compose build && docker compose up