#!/bin/bash
# Go to the directory where the script is located and build the docker image and run the container
cd ~/Documents/Automatic-Virtual-Meassurement-System/Server && docker compose build && docker compose up